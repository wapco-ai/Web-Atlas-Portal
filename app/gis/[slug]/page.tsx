import Link from "next/link";import {notFound} from "next/navigation";import {gisSolutions} from "../../lib/content";
export function generateStaticParams(){return gisSolutions.map(g=>({slug:g.slug}))}
export default async function GisDetail({params}:{params:Promise<{slug:string}>}){const{slug}=await params,g=gisSolutions.find(x=>x.slug===slug);if(!g)notFound();return <><header className="page-hero gis-detail"><Link href="/gis" className="back-link">راهکارهای مکانی ←</Link><i>{g.icon}</i><h1>راهکارهای مکانی / GIS و {g.title}</h1><p>{g.desc}</p></header><section className="section-shell solution-layout"><article className="reveal"><span>چشم‌انداز</span><h2>اطلاعات مکانی در خدمت مدیریت</h2><p>یکپارچه‌سازی داده‌های توصیفی و جغرافیایی، تصویری روشن از وضعیت موجود می‌سازد و امکان پایش، تحلیل و برنامه‌ریزی دقیق‌تر را فراهم می‌کند.</p></article><div className="capability-grid">{["بانک اطلاعات مکانی","نقشه‌های مدیریتی","داشبورد و گزارش","تحلیل و پایش","گردآوری داده میدانی","اتصال به سامانه‌های سازمان"].map((x,i)=><div className="reveal" key={x}><small>0{i+1}</small><b>{x}</b></div>)}</div></section><section className="home-cta"><span>راهکار متناسب با داده‌های سازمان شما</span><h2>برای جلسه تحلیل نیازمندی آماده‌ایم.</h2><Link href="/contact" className="button primary">درخواست مشاوره ←</Link></section></>}
