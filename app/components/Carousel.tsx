"use client";
/* eslint-disable @next/next/no-img-element */
import {useEffect,useState} from "react";import Link from "next/link";
const slides=[{image:"/assets/f1.png",title:"راهکارهای مکانی برای تصمیم‌های دقیق‌تر",text:"داده‌های پراکنده را به تصویری روشن و قابل اقدام تبدیل می‌کنیم.",href:"/gis"},{image:"/assets/f2.png",title:"سامانه‌های تخصصی مدیریت شهری",text:"راهکارهایی متناسب با فرایند واقعی سازمان‌های خدمات‌رسان.",href:"/products"},{image:"/assets/f3.png",title:"از تحلیل مسئله تا استقرار و پشتیبانی",text:"در تمام مسیر تحول دیجیتال کنار سازمان شما هستیم.",href:"/contact"}];
export default function Carousel(){const[i,setI]=useState(0);useEffect(()=>{const t=setInterval(()=>setI(v=>(v-1+slides.length)%slides.length),6000);return()=>clearInterval(t)},[]);return <section className="carousel reveal" aria-label="خدمات برگزیده">{slides.map((s,n)=><article className={i===n?"active":""} key={s.image}><img src={s.image} alt=""/><div><span>وب اطلس پویا</span><h2>{s.title}</h2><p>{s.text}</p><Link className="button primary" href={s.href}>بیشتر بدانید ←</Link></div></article>)}<div className="carousel-controls"><button onClick={()=>setI((i+1)%slides.length)}>→</button>{slides.map((_,n)=><button className={i===n?"active":""} onClick={()=>setI(n)} key={n} aria-label={`اسلاید ${n+1}`}/>) }<button onClick={()=>setI((i-1+slides.length)%slides.length)}>←</button></div></section>}
